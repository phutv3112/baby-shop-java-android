<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Màu nền của item -->
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray"/> <!-- Màu và độ rộng viền -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="8dp"
        android:bottom="8dp"/> <!-- Khoảng cách giữa viền và nội dung -->
    <corners android:radius="4dp"/> <!-- Bo tròn góc nếu cần -->
</shape>
